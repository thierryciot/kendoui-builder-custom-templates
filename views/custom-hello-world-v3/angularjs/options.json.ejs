<%#
    This is where we define which view properties will get added to the model in the controller.
%>
{
    "titleY": "<%- titleX.replace(/"/g, '\\"') %>",
    "greeting": "<%- greeting.replace(/"/g, '\\"') %>",
    "generatedAt": "<%- generatedAt %>",
    "version": "V3"
}
