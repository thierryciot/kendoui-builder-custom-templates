<%#
    This renders the view at runtime.
    The items defined in options.json.ejs are available from the model.
    The model is added to the variable vm.$model (view model) in the controller (See controller.js.ejs).

    In this template we can use AngularJS expressions, for example, {{vm.$model.title}} to access the title property.

    Note: This is an EJS comment - it won't show in the html output - See http://ejs.co/#docs for details
%>
<h2 ng-bind-html="vm.$model.title"></h2>

<div
        <% if (events.onClick) { %>
        ng-click="vm.<%- events.onClick %>($event)"
        <% } %>
>
    {{vm.$model.greeting}}&nbsp;World
</div>
Version: V2
