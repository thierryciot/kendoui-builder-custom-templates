<%#
    This is where we define which view properties and events handlers will get added to the model in the controller.
%>
{
    "title": "<%- title %>",
    "greeting": "<%- greeting %>",
    events: {
        onClick: (e) => {
        debugger;
        var that = this;
        var fct = this['onClick'];
        var fct2 = this['onClick2'];
        if ( fct !== undefined )
            fct(e);
        <% if (events.onClick) { %>
            this['<%- events.onClick %>'](e);
        <% } %>
        }
    }
}
